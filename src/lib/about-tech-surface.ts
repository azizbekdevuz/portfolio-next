/**
 * Matches the About / TechStack grid cell shell (`TechStack.tsx` — `rounded-xl border … bg-card`).
 * Tech marks use `TechIconTile` without an extra chip so the icon does not stack a second frame.
 */
export const ABOUT_TECH_CARD_SHELL =
  "rounded-xl border border-border bg-card shadow-sm dark:border-border-strong dark:bg-card";
