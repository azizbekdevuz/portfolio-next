/** localStorage key for explicit theme preference. */
export const THEME_STORAGE_KEY = "portfolio-theme-pref";

/** Values: absent = follow system; `light` | `dark` = locked. */
export type ThemePreference = "light" | "dark";
